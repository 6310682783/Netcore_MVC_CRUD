@model WebApplication1.Models.Movie

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h1>@Model.title</h1>
    <hr />
    <dl class="row">
        <dd class="col-sm-10">
            @*@Html.DisplayFor(model => model.url)*@
            <img src="@Model.url" alt="@Model.title" width="300" height="300">
        </dd>
    </dl>
</div>
<div>
    @*<a asp-page="./Edit" asp-route-id="@model.id">Edit</a> |
    <a asp-page="./Index">Back to List</a>*@
</div>